package com.example.salestaxes.exception;

/**
 * A type for the control types in our app
 * for now we need one (FORM_VALIDATION)
 * if we need another type we can add it here
 */
public enum ControlType {
    FORM_VALIDATION;
}
